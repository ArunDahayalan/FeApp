<Window x:Class="FEAPP.FEMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       ResizeMode="NoResize"
       WindowState="Maximized" 
       WindowStyle="None"
       WindowStartupLocation="CenterScreen"
       Height="{Binding SystemParameters.PrimaryScreenHeight}"
       Width="{Binding SystemParameters.PrimaryScreenWidth}">
    <Window.Background>
        <ImageBrush ImageSource="images\background1.png"></ImageBrush>
    </Window.Background>
    <DockPanel MinHeight ="{Binding SystemParameters.PrimaryScreenHeight}"  MinWidth ="{Binding SystemParameters.PrimaryScreenWidth}" Margin="0,0,0,0" x:Name="mainPanel" />
</Window>
